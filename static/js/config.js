// PhishGuard Frontend Configuration
// When deploying frontend to Netlify, set your Render backend URL below (e.g. "https://phishguard-backend.onrender.com")
// Or leave empty ("") and set it via the dashboard's "API" settings button or automatic routing.
window.PHISHGUARD_DEFAULT_BACKEND = "";
